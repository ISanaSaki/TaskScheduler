services:
  db:
    image: postgres:15
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: scheduler_db
    ports:
      - "5434:5432"

  redis:
    image: redis:7
    ports:
      - "6380:6379"